# SolTrade credentials — copy this file to .env and fill in the values.
# .env is git-ignored and must never be committed.
#
SOLTRADE_PRIVATE_KEY=
# Optional — only sent to Jupiter if set
SOLTRADE_JUPITER_API_KEY=
